#include<bits/stdc++.h>

using namespace std;

int main(){//Otimal page replacement. Did not solve
  long t;
  cin >> t;
  while(t--){
    long n,m;
    vector<long> cust[400]={0};
    long long order[m]={0};
    cin >> n >> m;
    long order[m];
    long table[n]={0};

    long clean=0;
    for(int i=0;i<m;i++){
      long x;
      cin >> order[i];
    }

    for(int i=0;i<m;i++){
      
    }

  }
}
