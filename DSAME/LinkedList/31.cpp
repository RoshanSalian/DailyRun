struct LinkedL *merg(struct LinkedL *head1, struct LinkedL *head2){
  struct LinkedL *head3, *newn;
  while(head1!=NULL && head2!=NULL){
    newn = malloc
    if(head1->data > head2->data){
      head3.insert(newn);
      head1 = head1->next;
    }
    else{
      //similar
    }
  }
}
