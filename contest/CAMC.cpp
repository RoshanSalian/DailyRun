#include<bits/stdc++.h>
using namespace std;
int main(){//TLE
  long t;
  // cin >> t;
  scanf("%ld", &t);
  while(t--){
    // long n, m;
    // // cin >> n >> m;
    // scanf("%ld%ld", &n, &m);long counter=0;
    // vector<pair<long, long>>v;
    // for(int i=0;i<n;i++){
    //   long x;
    //   scanf("%ld", &x);
    //   v.push_back({x, i});
    // }
    //
    // sort(v.begin(), v.end());
    //
    // vector<long> mini;
    // long a=INT_MAX, b=INT_MIN;
    // long check[m+1]={0};
    // // vector<long> res;
    //
    // long global_min=INT_MAX;
    // for(int i=0;i<v.size();i++){
    //   memset(check, 0, sizeof(check));
    //   long j=i;
    //   long count=0;
    //   long min=INT_MAX, max=INT_MIN;
    //   while(count<=m && j<v.size()){
    //     if( check[v[j].second%m]==0 ){
    //       check[v[j].second%m]=1;
    //       if(v[j].first<min)min=v[j].first;
    //       if(v[j].first>max)max=v[j].first;
    //       count++;
    //       if(min!=INT_MAX && max!=INT_MIN && count>1 && ((max-min)>global_min)){
    //         break;
    //       }
    //     }
    //     j++;
    //     if(count==m)break;
    //   }
    //   if(min!=INT_MAX && max!=INT_MIN && count==m && (max-min)<global_min ){
    //     global_min=(max-min);
    //   }
    // }
    // // cout << global_min<<"\n";
    // printf("%ld\n", global_min);

    long n, m;
    cin >> n >> m;
    for(int i=0;i<n;i++){
      
    }

  }
}
