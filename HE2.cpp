#include<bits/stdc++.h>
using namespace std;
int main(){
  long n, m;
  cin >> n >> m;
  long mod=1000000007;
  vector<long>arr;
  for(int i=0;i<n;i++){
    long x;
    cin >> x;
    arr.push_back(x);
  }
  sort(arr.begin(), arr.end());

  long i = v.size()-1;
  long sum=0;
  while(i>=m-1){
    sum+=
  }


}
